{
  "name": "fraud-shield",
  "version": "1.0.0",
  "private": true,
  "description": "Swifter Fraud Shield — Mobile Money Social Engineering Fraud Detection SDK",
  "workspaces": [
    "services/api",
    "dashboard"
  ],
  "scripts": {
    "vercel-build": "npm run vercel-build --workspace=dashboard",
    "dev:api": "npm run dev --workspace=services/api",
    "dev:dashboard": "npm run dev --workspace=dashboard",
    "build:dashboard": "npm run build --workspace=dashboard",
    "test:api": "npm run test --workspace=services/api",
    "test:dashboard": "npm run test --workspace=dashboard"
  },
  "devDependencies": {
    "docx": "^9.6.1",
    "exceljs": "^4.4.0"
  }
}
